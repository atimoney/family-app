# =============================================================================
# Family App - Web Frontend Environment Variables
# =============================================================================
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# IMPORTANT: Vite environment variables are baked at BUILD TIME.
# Changing these values requires a new build/deployment.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration (REQUIRED)
# -----------------------------------------------------------------------------

# API Base URL - Full URL to your backend API
# This is REQUIRED for the app to work. Without it:
# - In development: App will throw an error on startup
# - In production: API calls will fail (return index.html instead of JSON)
#
# Local development:
VITE_API_BASE_URL=http://localhost:3001
#
# Production/Staging (set in GitHub Environment variables):
# VITE_API_BASE_URL=https://ca-familyapi-staging.proudcliff-18049e77.australiasoutheast.azurecontainerapps.io

# -----------------------------------------------------------------------------
# Legacy/Minimals Template (optional)
# -----------------------------------------------------------------------------

# Server URL (used by Minimals template for mock endpoints)
# For family-app, use VITE_API_BASE_URL instead
VITE_SERVER_URL=

# Public resource directory
VITE_ASSETS_DIR=

# -----------------------------------------------------------------------------
# Firebase (optional - only if using Firebase auth)
# -----------------------------------------------------------------------------
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APPID=
VITE_FIREBASE_MEASUREMENT_ID=

# -----------------------------------------------------------------------------
# AWS Amplify (optional - only if using Amplify auth)
# -----------------------------------------------------------------------------
VITE_AWS_AMPLIFY_USER_POOL_ID=
VITE_AWS_AMPLIFY_USER_POOL_WEB_CLIENT_ID=
VITE_AWS_AMPLIFY_REGION=

# -----------------------------------------------------------------------------
# Auth0 (optional - only if using Auth0)
# -----------------------------------------------------------------------------
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_CALLBACK_URL=

# -----------------------------------------------------------------------------
# Supabase (REQUIRED for Supabase auth)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# App URLs (optional)
# -----------------------------------------------------------------------------
# VITE_APP_URL - defaults to window.location.origin
VITE_APP_URL=
# VITE_AUTH_REDIRECT_PATH - where to redirect after login, defaults to /family
VITE_AUTH_REDIRECT_PATH=/family
